# Project tracking

The organization-level delivery record is maintained in [`project-delivery-status.md`](./project-delivery-status.md).

Use that record with the Linear project `github.com/zed-pkg` and the organization GitHub Projects `zed-pkg-project` and `zed-pkg-test-project`.
